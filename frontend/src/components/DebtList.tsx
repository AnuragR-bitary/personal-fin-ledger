// DebtList is a legacy component superseded by the Debts & Transactions pages.
// Keeping it as a no-op stub to avoid breaking any residual imports.
export default function DebtList(_props: { refreshTrigger?: number }) {
    return null;
}
