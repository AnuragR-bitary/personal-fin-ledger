// RepaymentForm is a legacy component superseded by the Record Payment modal in Debts page.
// Keeping it as a no-op stub to avoid breaking any residual imports.
export default function RepaymentForm(_props: { debt?: unknown; onSuccess?: () => void; onCancel?: () => void }) {
    return null;
}
